interface:
  display_name: "MimiClaw Hardware Tools"
  short_description: "Design AI-callable GPIO, RGB, and sensor tools that map cleanly from chat phrasing."
  default_prompt: "Use this skill when designing MimiClaw hardware tools, names, schemas, and chat-trigger behavior."
